<UserControl x:Class="DiaporamaPlayer.PolaroidUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Name="uc">
    <Grid>
        <Rectangle MinHeight="200" MinWidth="200" RadiusX="10" RadiusY="10" Margin="5, 5, -5, -5">
            <Rectangle.Fill>
                <SolidColorBrush Color="Black" Opacity="0.3"/>
            </Rectangle.Fill>
        </Rectangle>
        <Rectangle Fill="#EEE" MinHeight="200" MinWidth="200" RadiusX="10" RadiusY="10" Stroke="#BBB" StrokeThickness="1"/>
        <Image Source="{Binding ElementName=uc, Path=PhotoSource}" Margin="20, 20, 20, 40"/>
    </Grid>
</UserControl>
